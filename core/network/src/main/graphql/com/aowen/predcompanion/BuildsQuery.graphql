query BuildsQuery($heroId: ID, $limit: Int, $offset: Int) {
    guidesPaginated(
        filter: { hero: $heroId }
        order: {
            field: SCORE,
            order: DESC
        }
        limit: $limit,
        offset: $offset
    ) {
        results {
            ...GuideFragment
        }
    }
}
