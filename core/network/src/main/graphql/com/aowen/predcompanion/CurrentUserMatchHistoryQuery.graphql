query CurrentUserMatchHistoryQuery(
    $limit: Int!,
    $offset: Int!,
    $filter: PlayerMatchesFilterInput
) {
    currentUser {
        players {
            ...MatchesPaginatedFragment
        }
    }
}