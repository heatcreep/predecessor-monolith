fragment MatchesPaginatedFragment on Player {
    matchesPaginated(
        limit: $limit,
        offset: $offset,
        filter: $filter
    ) {
        totalCount
        ...MatchResultsFragment
    }
}